When running, ask to delete install/ directory.

Decorate the exeception pages
	- eg. Unregistered pages being accessed

Check installation on a symlinked location

Put .htaccess files in each seaprate folders

Produce database log files in REALPATH of public_html parent only.
	- eg. deals when symlinked into other locations

When license file exists, do not produce config files.
Whe install directory exists, do not run the website.
Create .ini file of the file to read back the configurations.
Relocate license file in library directory.
Do not serve license.ini in .htaccess.
Create default users:
	- admin
	- active user
	- disabled user

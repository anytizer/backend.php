/*function init() {
 calendar.set("birthday");
 calendar.set("anotherday");
 }
 */

// if function init() is not used, then call the date picker manually
calendar.set("birthday");
calendar.set("anotherday");
